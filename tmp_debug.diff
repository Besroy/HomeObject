diff --git a/src/lib/homestore_backend/heap_chunk_selector.cpp b/src/lib/homestore_backend/heap_chunk_selector.cpp
index a4d4670..b46d4ab 100644
--- a/src/lib/homestore_backend/heap_chunk_selector.cpp
+++ b/src/lib/homestore_backend/heap_chunk_selector.cpp
@@ -59,6 +59,10 @@ csharedChunk HeapChunkSelector::select_chunk(homestore::blk_count_t count, const
         uint32_t application_hint = hint.application_hint.value();
         pg_id_t pg_id = (uint16_t)(application_hint >> 16 & 0xFFFF);
         homestore::chunk_num_t v_chunk_id = (uint16_t)(application_hint & 0xFFFF);
+
+        LOGWARNMOD(homeobject, "get create shard hint:  v_chunk_id {}, pg {}, application_hint", v_chunk_id, pg_id,
+                   application_hint);
+
         return select_specific_chunk(pg_id, v_chunk_id);
     }
 }
diff --git a/src/lib/homestore_backend/hs_shard_manager.cpp b/src/lib/homestore_backend/hs_shard_manager.cpp
index 6768192..8cc4190 100644
--- a/src/lib/homestore_backend/hs_shard_manager.cpp
+++ b/src/lib/homestore_backend/hs_shard_manager.cpp
@@ -116,6 +116,8 @@ ShardManager::AsyncResult< ShardInfo > HSHomeObject::_create_shard(pg_id_t pg_ow
     if (!v_chunkID.has_value()) {
         LOGW("no availble chunk left to create shard for pg [{}]", pg_owner);
         return folly::makeUnexpected(ShardError::NO_SPACE_LEFT);
+    } else {
+        LOGW("create shard with v_chunkID = [{}], in pg = [{}]", v_chunkID, pg_owner);
     }
 
     // Prepare the shard info block
diff --git a/src/lib/homestore_backend/replication_state_machine.cpp b/src/lib/homestore_backend/replication_state_machine.cpp
index 35f5deb..c315a9b 100644
--- a/src/lib/homestore_backend/replication_state_machine.cpp
+++ b/src/lib/homestore_backend/replication_state_machine.cpp
@@ -146,7 +146,7 @@ ReplicationStateMachine::get_blk_alloc_hints(sisl::blob const& header, uint32_t
         pg_id_t pg_id = msg_header->pg_id;
         // check whether the pg exists
         if (!home_object_->pg_exists(pg_id)) {
-            LOGI("can not find pg {} when getting blk_alloc_hint", pg_id);
+            LOGW("can not find pg {} when getting blk_alloc_hint", pg_id);
             // TODO:: add error code to indicate the pg not found in homestore side
             return folly::makeUnexpected(homestore::ReplServiceError::NO_SPACE_LEFT);
         }
@@ -162,6 +162,9 @@ ReplicationStateMachine::get_blk_alloc_hints(sisl::blob const& header, uint32_t
         static_assert(std::is_same< homestore::chunk_num_t, uint16_t >::value, "chunk_num_t is not uint16_t");
         homestore::chunk_num_t v_chunk_id = v_chunkID.value();
         hints.application_hint = ((uint64_t)pg_id << 16) | v_chunk_id;
+
+        LOGW("set create shard hint:  v_chunk_id {}, pg {}, application_hint", v_chunkID.value(), pg_id,
+             hints.application_hint.value());
         return hints;
     }
 
